const nav=document.querySelector('.nav'),menu=document.getElementById('menu');menu.onclick=()=>nav.classList.toggle('open');document.querySelectorAll('#links a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));
const obs=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>obs.observe(x));
const glow=document.querySelector('.cursor-glow');addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});
const v=document.getElementById('visual');if(v&&matchMedia('(pointer:fine)').matches){v.onpointermove=e=>{let r=v.getBoundingClientRect(),x=e.clientX/r.width-.5,y=e.clientY/r.height-.5;v.style.transform=`rotateY(${x*5}deg) rotateX(${-y*5}deg)`};v.onpointerleave=()=>v.style.transform=''}
const sections=[...document.querySelectorAll('section[id]')],links=[...document.querySelectorAll('#links a')];addEventListener('scroll',()=>{let c='home';sections.forEach(s=>{if(scrollY>=s.offsetTop-180)c=s.id});links.forEach(a=>a.classList.toggle('active',a.hash==='#'+c))});
